<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to root_path, class: "navbar-brand" do %>
        Състезание "Акад. Л. Чакалов"
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="main-navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Начало", root_path %></li>
        <% if user_signed_in? %>
          <% if current_user.is_parent? %>
            <li>
              <%= link_to "Ученици", students_path %>
            </li>
          <% else %>
            <li>
              <%= link_to "Одобрения", approvals_path %>
            </li>
            <li>
              <% if current_user.has_role? :admin %>
                <%= link_to "Администрация", rails_admin_path %>
              <% end %>
            </li>
          <% end %>

          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.full_name %>
              <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Изход", destroy_user_session_path, :method => :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Вход", new_user_session_path %></li>
          <li><%= link_to "Регистрация", new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
